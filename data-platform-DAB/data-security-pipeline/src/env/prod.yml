# Prod environment permissions
# Included by databricks.yml via: include: src/env/*.yml

targets:
  prod:
    permissions:
      - user_name: ${workspace.current_user.userName}
        level: CAN_MANAGE
